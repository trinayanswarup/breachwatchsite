import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'NordVPN vs ExpressVPN 2025 — Which Is Actually Better?',
  description:
    'Side-by-side comparison of NordVPN and ExpressVPN on every criterion: price, logging policy, speed, jurisdiction, and streaming. Clear verdict with reasoning.',
};

export default function NordVpnVsExpressVpnPage() {
  return null;
}
