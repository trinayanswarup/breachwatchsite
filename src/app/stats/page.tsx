import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Site Stats — Traffic and Analytics',
  description:
    'Weekly traffic stats for BreachWatch. We publish our analytics publicly as a commitment to commercial transparency.',
};

export default function StatsPage() {
  return null;
}
