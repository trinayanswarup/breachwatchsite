import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Privacy Policy',
  description:
    'BreachWatch privacy policy. We use Vercel Analytics (no cookies, anonymous). We collect no email addresses and store no personal data.',
};

export default function PrivacyPage() {
  return null;
}
