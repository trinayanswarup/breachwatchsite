import type { Product, Criterion } from '@/lib/types';

export interface ScoreBreakdownProps {
  product: Product;
  criteria: Criterion[];
}

export default function ScoreBreakdown(_props: ScoreBreakdownProps) {
  return null;
}
